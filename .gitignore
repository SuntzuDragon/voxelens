# Rust build artifacts
/target

# CLI stage dumps and scratch output (generated, never committed)
/out

# Extracted Mojang textures are NOT redistributable. Load them from a local
# Minecraft install at runtime. Only the self-made stand-in tiles are tracked.
/textures/*
!/textures/stand-in/

# OS / editor noise
.DS_Store
*.swp
